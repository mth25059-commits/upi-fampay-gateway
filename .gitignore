# Secrets and local config — never commit these.
.env
.env.*
!.env.example

# Real bank-alert samples carry a real name, amount and UTR. Only the
# sanitised *.example.eml is allowed in.
samples/*.eml
!samples/*.example.eml

# Node / editor noise
node_modules/
npm-debug.log*
.DS_Store
*.log
.idea/
.vscode/
.claude/
